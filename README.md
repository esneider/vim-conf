### To install:

```
git clone https://github.com/esneider/vim_conf.git
./vim_conf/install.sh
```

### Dependencies:

* vim (compiled with +python)
* git

#### Optionals (for plugins)

* exuberant ctags
* curl / wget
* perl

